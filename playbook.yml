site:
  title: Unity ECS HOWTOs
  start_page: main_component::overview.adoc
content:
  sources:
    - url: ./
      branches: HEAD
ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/master/raw/build/ui-bundle.zip?job=bundle-stable #default Antora GUI, we can specify our own skins if we want to
    snapshot: true